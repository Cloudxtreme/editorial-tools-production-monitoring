stage = "production"

code {
  composer-base-url = "https://composer.code.dev-gutools.co.uk"

  panda {
    domain = "code.dev-gutools.co.uk"
    system = "editorialToolsTest"
  }
}

production {
  composer-base-url = "https://composer.gutools.co.uk"

  panda {
    domain = "gutools.co.uk"
    system = "editorialToolsTest"
  }
}

include "application.local"